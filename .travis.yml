language: node_js
node_js:
  - "stable"
dist: focal

before_script:
  - cd $TRAVIS_BUILD_DIR

script:
  - npm install
  - npm test

deploy:
  provider: pages
  skip_cleanup: true
  github_token: $github_token
  local_dir: build
  on:
    branch: main
